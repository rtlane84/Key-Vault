{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:push": "pnpm --filter @workspace/db run push",
    "db:backfill": "pnpm --filter @workspace/scripts run saas-backfill",
    "api:build": "pnpm --filter @workspace/api-server run build",
    "api:start": "pnpm --filter @workspace/api-server run start",
    "dashboard:build": "pnpm --filter @workspace/dashboard run build",
    "storefront:build": "pnpm --filter @workspace/storefront run build"
  },
  "private": true,
  "devDependencies": {
    "@rollup/rollup-darwin-arm64": "4.60.3",
    "lightningcss": "1.32.0",
    "lightningcss-darwin-arm64": "1.32.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
